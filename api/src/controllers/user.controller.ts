import { Request, Response, RequestHandler } from 'express';
import ServerResponse from '../interfaces/ServerResponse.interface';

export const userPersonalInfoUpdate: RequestHandler = async (req: Request, res: Response) => {

}